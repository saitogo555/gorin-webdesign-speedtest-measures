# C7: 配列操作と統計計算

PHP の配列関数を使って統計情報を計算・表示しなさい。

- 以下の配列を用意すること：`[85, 92, 78, 95, 88, 72, 96, 84, 90, 77]`
- `array_sum()` で合計、`count()` で個数、割り算で平均を計算すること
- `max()` と `min()` で最高点・最低点を求めること
- `sort()` で昇順に並び替えて表示すること
- 平均点以上の数値のみを `array_filter()` で抽出して表示すること
- 各値を `array_map()` で10%加算した新しい配列を表示すること
