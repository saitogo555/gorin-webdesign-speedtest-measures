# C8: 正規表現を使った入力バリデーション

PHP の正規表現（`preg_match()`）を使ってフォームバリデーションを実装しなさい。

- 以下の4フィールドを持つフォームを作成すること
  - 郵便番号（`123-4567` 形式）
  - 電話番号（`090-1234-5678` または `03-1234-5678` 形式）
  - メールアドレス
  - パスワード（英大文字・小文字・数字をそれぞれ1文字以上含む8文字以上）
- 各フィールドに正規表現バリデーションを実装すること
- エラー時は入力値を保持した状態でエラーメッセージを表示すること
