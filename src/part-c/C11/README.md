# C11: 日付・時刻操作

PHP の `DateTime` クラスと日付関数を使った日付計算ツールを作成しなさい。

- 開始日と終了日を入力できること
- 2つの日付の差（日数・週数・月数）を計算して表示すること
- 入力された日付の曜日を日本語で表示すること
- 現在日時から入力日付までのカウントダウン（残り日数）を表示すること
- うるう年かどうかを判定して表示すること
- 日付のバリデーション（存在する日付かどうか）を `checkdate()` で行うこと
