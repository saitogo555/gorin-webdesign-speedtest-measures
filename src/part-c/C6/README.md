# C6: クッキーを使ったユーザー設定保存

PHP のクッキー機能を使って、ユーザーの表示設定を保存・適用する機能を実装しなさい。

- フォントサイズ（小・中・大）と背景テーマ（ライト・ダーク）を選択できること
- 設定保存ボタンで `setcookie()` を使い、30日間有効なクッキーに保存すること
- ページ再読み込み・再訪問時にクッキーの設定が自動で適用されること
- クッキーが存在しない場合はデフォルト設定を使用すること
- 設定のリセットボタンでクッキーを削除すること
