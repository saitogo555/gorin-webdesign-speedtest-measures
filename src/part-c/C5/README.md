# C5: CSVファイルの読み込みとテーブル表示

PHP を使って CSV ファイルを読み込み、HTMLテーブルとして表示しなさい。

- `fgetcsv()` を使用してCSVを読み込むこと
- 1行目をヘッダー行として `<th>` で表示すること
- 2行目以降をデータ行として `<td>` で表示すること
- テーブルの列ヘッダーをクリックするとその列で昇順・降順ソートができること（PHP処理）
- XSSを防ぐために `htmlspecialchars()` を使用すること
