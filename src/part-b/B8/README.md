# B8: Intersection Observer によるスクロールアニメーション

`IntersectionObserver` API を使用して、スクロール連動アニメーションを実装しなさい。

- ページに5つ以上のカード要素を縦に配置すること
- 各カードがビューポートに入ったときにフェードイン＋スライドインするアニメーションを付けること
- `IntersectionObserver` の `threshold` オプションを使用すること
- 一度表示されたカードは再スクロールしてもアニメーションが繰り返されないこと
