# B23: `storage` イベントを使ったタブ間通信

`storage` イベントを使って、複数タブ間でリアルタイムにデータを共有する機能を実装しなさい。

- テキスト入力フォームに入力した内容を `localStorage` に保存すること
- 別タブで同じページを開いた際に、`storage` イベントで変更を検知してリアルタイムに表示を更新すること
- 最後にデータを更新したタブIDと時刻を表示すること
- 「クリア」ボタンで全タブのデータを削除すること
