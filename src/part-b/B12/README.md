# B12: `scroll` イベントを使った目次ハイライト

`scroll` イベントを使って、スクロール位置に応じて目次をハイライトするUIを実装しなさい。

- ページに4セクション以上を配置すること
- 固定の目次（サイドバー）を用意すること
- スクロールで現在表示中のセクションに対応する目次リンクがハイライトされること
- 目次リンクをクリックすると対応セクションにスムーズスクロールすること（`scrollIntoView`）
