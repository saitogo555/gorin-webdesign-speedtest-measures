# B13: `focus` / `blur` イベントを使ったフォームUI

`focus` と `blur` イベントを活用して、インタラクティブなフォームを実装しなさい。

- 名前・メールアドレス・パスワードの3フィールドを作成すること
- フォーカス時にラベルがアニメーションでフィールド上部に移動するフローティングラベルを実装すること
- `blur` 時にバリデーションを実行し、エラーメッセージを表示すること
- パスワードフィールドは表示・非表示を切り替えるトグルボタンを付けること
