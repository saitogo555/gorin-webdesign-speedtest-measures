# B40: compositionstart/compositionendを使った日本語入力対応サジェスト

以下の要件を満たす日本語入力対応の検索サジェストを作成しなさい。

- `input` イベントで文字列を検索し候補リストを表示する通常のサジェスト機能を実装すること
- `compositionstart` イベントでIME変換中フラグを立て、変換中は候補リストを更新しないこと
- `compositionend` イベントでIME変換完了を検知し、確定後に候補リストを更新すること
- 候補データは配列で10件以上用意すること
