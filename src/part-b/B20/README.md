# B20: `beforeunload` イベントを使った離脱確認

`beforeunload` イベントを使って、ページ離脱時に確認ダイアログを表示する機能を実装しなさい。

- テキストエリアに何か入力した後でページを離れようとすると確認ダイアログが出ること
- テキストエリアが空の場合はダイアログを表示しないこと
- 「保存済み」ボタンをクリックするとダイアログが表示されなくなること
- `window.addEventListener('beforeunload', ...)` を使用すること
