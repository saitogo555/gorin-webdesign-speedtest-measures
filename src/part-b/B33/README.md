# B33: ResizeObserverを使ったコンテンツサイズ表示

以下の要件を満たすコンテンツサイズモニターを作成しなさい。`MutationObserver` は使用せず、**`ResizeObserver`** を使用すること。

- ドラッグでサイズ変更可能な要素（`resize: both; overflow: auto`）を1つ作成すること
- `ResizeObserver` でその要素を監視し、リサイズのたびに現在のサイズ（幅・高さ）をリアルタイムで表示すること
- サイズの最小値・最大値の履歴を画面に保持し、3件以上になった場合は古いものから削除すること
