# B15: `wheel` イベントを使った3Dカルーセル

`wheel` イベントを使って、マウスホイールで操作できるカルーセルを実装しなさい。

- カード要素を5枚以上円形（3D回転）に配置すること
- ホイールスクロールで回転方向が変わること
- `transform: rotateY()` と `perspective` を使った3D表現にすること
- 現在選択中のカードが拡大・前面表示されること
