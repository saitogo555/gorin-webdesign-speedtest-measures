# B24: `transitionend` イベントを使った連鎖トランジション

`transitionend` イベントを使って、複数要素を連鎖的にトランジションさせる機能を実装しなさい。

- 「開始」ボタンクリックで最初の要素のトランジションが始まること
- 各要素のトランジション完了（`transitionend`）を検知して次の要素のトランジションを開始すること
- 要素は6つ以上用意し、色・大きさ・位置など異なるプロパティを変化させること
- 「リセット」ボタンで全要素を初期状態に戻すこと
