# B48: タブ切り替えパネル

JavaScriptを用いて、タブをクリックするとコンテンツが切り替わるUIを実装しなさい。

- タブを4つ用意する（「ホーム」「プロフィール」「スキル」「お問い合わせ」）
- 初期状態で「ホーム」タブが選択されており、対応するコンテンツが表示される
- タブをクリックすると対応するコンテンツに切り替わる
- 選択中のタブにはアクティブスタイル（下線、背景色の変化など）を適用する
- コンテンツの切り替えにはCSSの `display` を `none` / `block` で制御すること
- 各コンテンツパネルは最低でも2行以上のテキストを含めること
