# A23: ネオモーフィズムカード

以下の要件を満たすネオモーフィズムデザインのカードコンポーネントを作成しなさい。JavaScriptは利用できない。

- 背景色は `#e0e5ec` とすること
- カードのサイズは `W:200px × H:200px` とし、中央に任意のテキストを表示すること
- `box-shadow` を2方向に設定し、明るい影と暗い影でネオモーフィズムを表現すること
- ホバー時にカードが凹んだ状態（inset shadow）に変化するアニメーションを実装すること
- アニメーションのトランジション時間は `0.3s` とすること
