# A22: CSSのみのスクロールスナップ

CSS スクロールスナップを使った縦スクロールのページを作成しなさい。

- `scroll-snap-type` と `scroll-snap-align` を使用すること
- 4セクション以上を用意し、スクロール時に各セクションにスナップすること
- 各セクションはビューポート全体（`100vh`）の高さを持つこと
- 各セクションに異なる背景色またはグラデーションを適用すること
