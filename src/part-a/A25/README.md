# A25: CSSのみの通知バッジ付きアイコン

以下の要件を満たす通知バッジUIを作成しなさい。JavaScriptは利用できない。

- 任意の絵文字またはテキストを使ったアイコン要素を作成すること
- アイコンの右上に赤色の円形バッジを `::after` 擬似要素のみで実装すること
- バッジ内には数字「3」を表示すること
- `position: relative` と `position: absolute` を使い、アイコンに重なるよう配置すること
