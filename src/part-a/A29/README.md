# A29: aspect-ratioを使ったレスポンシブカードグリッド

以下の要件を満たすレスポンシブなカードグリッドを作成しなさい。JavaScriptは利用できない。

- CSS Grid の `auto-fill` または `auto-fit` とともに `minmax()` を使い、カラム数がブラウザ幅に合わせて自動調整されること
- 各カードは `aspect-ratio: 1 / 1` で正方形を保つこと
- カードには背景色とテキストを含めること
- `gap` プロパティで余白を設定すること
