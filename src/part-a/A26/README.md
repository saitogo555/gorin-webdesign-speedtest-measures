# A26: CSSグリッドによるモザイクギャラリー

以下の要件を満たすモザイク状のギャラリーレイアウトを作成しなさい。JavaScriptは利用できない。

- CSS Grid を使用して最低6つのセルを配置すること
- 一部のセルは `grid-column` や `grid-row` で複数セルをまたがるよう設定すること
- 各セルには異なる背景色を設定し、セル内にテキストを中央配置すること
- レイアウトはデスクトップ（`W:1280px`）で正しく表示されること
