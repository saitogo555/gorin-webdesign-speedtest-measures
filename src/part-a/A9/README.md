# A9: CSSカスタムプロパティ（変数）を使ったテーマUI

CSS カスタムプロパティを活用してテーマを切り替えられるカードUIを作成しなさい。

- `:root` にカラーパレット用カスタムプロパティを定義すること
- `.theme-ocean`、`.theme-forest`、`.theme-sunset` の3テーマクラスを用意すること
- 各テーマでカスタムプロパティの値を上書きし、カードの見た目が変わること
- カードには画像・タイトル・説明文・ボタンを含めること
