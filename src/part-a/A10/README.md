# A10: CSSのみのモーダル風オーバーレイ

JavaScript を使用せず、CSS と HTML のみでモーダル風のオーバーレイを実装しなさい。

- `:target` 疑似クラスを使用すること
- 「開く」ボタンクリックでオーバーレイが表示されること
- オーバーレイの背景（半透明の暗い背景）が表示されること
- 「閉じる」ボタンまたは外側クリックで閉じられること
- 表示・非表示の切り替えにトランジションを付けること
