# A42: printメディアクエリを使った印刷スタイル

以下の要件を満たす印刷用スタイルシートを作成しなさい。JavaScriptは利用できない。

- 通常表示ではナビゲーションバー・サイドバー・フッターが表示されること
- `@media print` 内でナビゲーションバー・サイドバー・フッターを非表示にすること
- 印刷時に本文エリアが全幅（`100%`）で表示されること
- 印刷時に全リンクのURLが `::after` 擬似要素で本文中に表示されること（例：`content: " (" attr(href) ")"` を使用）
