# A45: CSS のみのステップフォームUI

CSSのみ（JavaScriptは利用しない）で、以下のステップ進捗バーUIを作成しなさい。

- ステップは「STEP 1」「STEP 2」「STEP 3」「STEP 4」の4つを横並びに表示する
- 各ステップは円形のアイコンと下部のラベルテキストで構成される
- 現在のステップ（2番目）は強調表示（例: 青、拡大など）する
- 完了済みのステップ（1番目）は塗りつぶし表示（例: 灰色）にする
- 未完了のステップ（3・4番目）は枠線のみの表示にする
- 各ステップ間を横線で繋ぐこと
