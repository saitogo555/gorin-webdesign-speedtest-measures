# A41: backdrop-filterを使ったガラスモーフィズムカード

以下の要件を満たすガラスモーフィズムデザインのカードを作成しなさい。JavaScriptは利用できない。

- 背景にはグラデーションやカラフルな図形（CSSで生成）を配置すること
- カードには `background: rgba(255, 255, 255, 0.1)` などの半透明背景を設定すること
- カードに `backdrop-filter: blur(10px)` を適用し、背景をぼかしたガラス効果を実現すること
- カードの枠線には半透明の白を使うこと
