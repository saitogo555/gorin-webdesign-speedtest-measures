# A13: レスポンシブな画像ギャラリー

CSS Grid または Flexbox を使用して、レスポンシブな画像ギャラリーを作成しなさい。

- 画像は6枚以上配置すること（`background-color` でダミー表示でも可）
- PC表示では4カラム、タブレットでは2カラム、スマートフォンでは1カラムにすること
- 画像にホバーすると拡大（`transform: scale`）するアニメーションを付けること
- 各画像の下にキャプションを表示すること
