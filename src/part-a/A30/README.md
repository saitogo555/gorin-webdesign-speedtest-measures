# A30: CSSのみのスライドインサイドバー

以下の要件を満たすサイドバーをCSSとHTMLのみで作成しなさい。JavaScriptは利用できない。

- チェックボックスの `:checked` 状態を利用してサイドバーの開閉を制御すること
- サイドバーはページ左側から `translateX` でスライドインすること
- 開閉ボタン（ハンバーガーアイコン相当）はCSSで描画すること
- サイドバーが開いている間、メインコンテンツが視覚的に押し出されるか半透明オーバーレイが表示されること
