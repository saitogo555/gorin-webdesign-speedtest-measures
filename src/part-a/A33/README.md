# A33: CSSのみのセグメント型プログレスバー

以下の要件を満たすプログレスバーを作成しなさい。JavaScriptは利用できない。シンプルな単一バーではなく、各ステップに区切りが入る**セグメント型**プログレスバーとすること。

- 5つのセグメント（区切り）を持つプログレスバーを作成すること
- ラジオボタンの `:checked` を使い、どのセグメントまで完了したかを視覚的に表現すること
- 完了済みセグメントは色が変わること
- セグメント間の境界線はCSSのみで描画すること
