# A31: CSSのみのツールチップ

以下の要件を満たすツールチップをCSSとHTMLのみで作成しなさい。JavaScriptは利用できない。

- 3つ以上のボタンまたはリンクを配置し、それぞれにツールチップテキストを設定すること
- ツールチップは `::after`（または `::before`）と `data-tooltip` 属性のみで実装すること（`content: attr(data-tooltip)` を使用すること）
- ホバー時にツールチップがフェードインして表示されること
- ツールチップにはキャレット（三角形の吹き出し矢印）を付けること
