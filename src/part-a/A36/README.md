# A36: スクロール駆動アニメーションによる進捗バー

以下の要件を満たすスクロール進捗インジケーターを作成しなさい。JavaScriptは利用できない。

- ページ上部に固定された細いバー（高さ `4px`、幅は初期 `0%`）を配置すること
- CSS の `@keyframes` と `animation-timeline: scroll()` を使用し、スクロール量に応じてバーの幅が `0%` から `100%` に変化すること
- ページ内に十分なスクロール量を確保するためのコンテンツ（ダミーテキスト等）を用意すること
- バーの色は任意とするが、視認性を確保すること
