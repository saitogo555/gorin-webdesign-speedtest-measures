# A35: :targetを使ったタブUI

以下の要件を満たすタブUIを作成しなさい。JavaScriptは利用できない。`input[type="checkbox/radio"]` は使用せず、**`<a>` タグのハッシュリンクと `:target` 擬似クラスのみ**を使用すること。

- タブは3つ以上作成すること
- タブをクリックすると対応するコンテンツが表示され、他のコンテンツは非表示になること
- アクティブなタブは下線またはボーダーで強調表示されること
- ページの初期表示時は最初のタブの内容が表示されること
