# A21: SVGアニメーション付きローダー

SVG と CSS を組み合わせたローダーアニメーションを作成しなさい。

- SVG要素をHTMLに直接記述すること（インラインSVG）
- `<circle>` または `<path>` を使ったアニメーションにすること
- `stroke-dasharray` と `stroke-dashoffset` を変化させるアニメーションを実装すること
- アニメーションは CSS `@keyframes` で制御すること
- JavaScriptは利用できない
