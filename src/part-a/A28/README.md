# A28: CSSカウンターを使った階層番号付きリスト

以下の要件を満たす階層構造の番号付きリストを作成しなさい。JavaScriptは利用できない。

- `counter-reset` と `counter-increment` を用いて実装すること
- 第1階層は `1.`、`2.`、`3.` と表示されること
- 第2階層は `1.1.`、`1.2.`、`2.1.` のような複合番号で表示されること
- 番号は `::before` 擬似要素で出力し、HTMLにはリスト番号を直接記述しないこと
- 3階層以上を含むリストを作成すること
