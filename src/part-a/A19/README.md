# A19: フォームのバリデーションスタイル

CSS 疑似クラスを活用したフォームバリデーションUIを作成しなさい。

- `input:valid`、`input:invalid`、`input:focus` 疑似クラスを使用すること
- 名前・メールアドレス・電話番号の3フィールドを作成すること
- 入力値が有効な場合は緑のボーダー、無効な場合は赤のボーダーを表示すること
- フォーカス時には `outline` をカスタムデザインにすること
- `required` 属性と `pattern` 属性を組み合わせること
