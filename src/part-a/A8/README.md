# A8: CSSのみのドロップダウンメニュー

JavaScriptを使わず、CSS のみでドロップダウンナビゲーションを作成しなさい。

- 親メニューに3項目以上を配置すること
- 各親項目にホバーするとサブメニューが表示されること
- サブメニューは `opacity` と `visibility` を使ったトランジションで表示・非表示を切り替えること
- サブメニューが親メニューの幅より広い場合は右端からはみ出さないようにすること
