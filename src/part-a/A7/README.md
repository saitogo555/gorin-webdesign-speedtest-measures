# A7: CSSのみのローディングアニメーション

JavaScriptを使わず、CSS アニメーションのみでローディング画面を作成しなさい。

- スピナー（回転する円形）を表示すること
- `@keyframes` と `animation` プロパティを使用すること
- アニメーションは無限ループすること
- スピナーの色が `animation` によって変化すること
- 画面中央に表示されること
