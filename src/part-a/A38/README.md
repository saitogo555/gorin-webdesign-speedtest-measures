# A38: mix-blend-modeを使ったテキストオーバーレイ

以下の要件を満たすテキストオーバーレイエフェクトを作成しなさい。JavaScriptは利用できない。

- 背景には任意のグラデーションまたはカラフルな図形（CSSで生成）を配置すること
- その上に白色の大きなテキストを重ね、`mix-blend-mode: difference` または `multiply` を適用すること
- テキストが背景色に応じて色反転するエフェクトが視覚的に確認できること
