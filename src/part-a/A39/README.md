# A39: CSSのみの折りたたみ可能なナビゲーションツリー

以下の要件を満たすツリー型ナビゲーションをCSSとHTMLのみで作成しなさい。JavaScriptは利用できない。

- 親項目クリックで子項目が展開・収納できること（`input[type="checkbox"]` と `:checked` を使用すること）
- 2階層以上のネストを含むこと
- 展開・収納はCSSトランジションでスムーズに動作すること
- 展開中の親項目には `▼`、収納中には `▶` のインジケーターを `::before` 擬似要素と `content` で表示すること
