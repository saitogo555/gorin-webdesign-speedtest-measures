# A34: clamp()を使ったレスポンシブタイポグラフィ

以下の要件を満たすレスポンシブなタイポグラフィのデモページを作成しなさい。JavaScriptは利用できない。

- `font-size` に `clamp()` 関数を使用し、最小・推奨・最大フォントサイズを設定すること
- 見出し（`h1`）・副見出し（`h2`）・本文（`p`）の3段階でそれぞれ異なる `clamp()` 値を設定すること
- ブラウザ幅を変更するとフォントサイズが滑らかに変化することを確認できること
