# A32: CSSのみのスケルトンスクリーン

以下の要件を満たすスケルトンスクリーン（読み込み中の骨格表示）をCSSとHTMLのみで作成しなさい。JavaScriptは利用できない。

- カード型UIのスケルトン（サムネイル部分・タイトル部分・本文部分）を3つ以上横並びに配置すること
- `background: linear-gradient` とCSSアニメーション（`@keyframes`）を使ったシマーエフェクト（左から右へ光が流れるアニメーション）を実装すること
- アニメーションは無限ループすること
- 配色はグレー系とすること
